'use strict';

/**
 * release-note-tag controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::release-note-tag.release-note-tag');

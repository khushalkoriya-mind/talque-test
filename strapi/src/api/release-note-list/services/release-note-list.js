'use strict';

/**
 * release-note-list service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::release-note-list.release-note-list');

'use strict';

/**
 * tradeshow-trust-logo controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::tradeshow-trust-logo.tradeshow-trust-logo');

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false,
  images: {
    domains: ["localhost", "loved-symphony-cf7407a623.media.strapiapp.com"],
  },
};

module.exports = nextConfig;
